/**
 * Local type stub for @prisma/adapter-pg.
 * The real package is installed by npm/Vercel at build time.
 * Remove this file if @prisma/adapter-pg is installed locally.
 */
declare module "@prisma/adapter-pg" {
  export class PrismaPg {
    constructor(options: { connectionString: string });
  }
}
